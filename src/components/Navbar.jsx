import React from "react";
import '../index.css'; // Include the CSS file for styles

const Navbar = () => {
  return (
    <header className="navbar">
      <div className="">
        <h1 className="navbar-title">FlashCard.AI</h1>
      </div>
    </header>
  );
};

export default Navbar;
